# Начинаем с базового образа с установленным Java 17
FROM openjdk:17-jdk-slim

# Устанавливаем рабочую директорию для нашего приложения
WORKDIR /app

# Копируем jar-файл с приложением внутрь контейнера
COPY target/server-0.0.1-SNAPSHOT.jar /app/server.jar

# Открываем порт, который будет использоваться приложением
EXPOSE 8080

# Запускаем приложение при старте контейнера
CMD ["java", "-jar", "/app/server.jar"]